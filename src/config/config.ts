export const config = {
    PORT: process.env.PORT || 3000,
    DB_NAME: process.env.DB_NAME || 'school',
    DB_USER: process.env.DB_USER || 'root',
    DB_PASSWORD: process.env.DB_PASSWORD || 'root',
    DB_HOST: process.env.DB_HOST || 'localhost',
}